dist: xenial
language: python
python:
  - "3.7"
install: 
  - pip install .
  - pip install --user -r requirements.txt
  - pip install pytest-cov
  - pip install coveralls
script:
  - pytest --cov=obia4rtm/
after_success:
  - coveralls
